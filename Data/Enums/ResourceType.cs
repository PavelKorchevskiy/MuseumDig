public enum ResourceType
{
	Bone,   // Кость (часть коллекции) — есть качество
	Tooth,  // Зуб (выставляется отдельно) — есть качество
	Gold,   // Золото — только продажа
	Gem     // Драгоценный камень — только продажа
}
